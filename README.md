# TasKing

## API

Return all tasks
GET: `/api/tasks`

Return task by id
GET: `/api/tasks/{task-id}`

Return all task histories
GET: `/api/history/{task-id}`

Save task
POST: `/api/tasks`

Save history entry
POST: `/api/history`